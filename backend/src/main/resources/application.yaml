spring:
  application:
    name: LethalFinance

  datasource:
    url: jdbc:postgresql://localhost:5435/lethalFinance
    username: user
    password: pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

server:
  port: 8080
