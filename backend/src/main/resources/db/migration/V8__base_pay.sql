CREATE TABLE base_pay (
                          rank        VARCHAR NOT NULL,
                          yos_min     INTEGER NOT NULL,
                          yos_max     INTEGER NOT NULL,
                          monthly_pay NUMERIC(10,2) NOT NULL
);